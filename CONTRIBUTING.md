# 贡献事实修正

欢迎修正项目身份、版本、来源、术语和平台验证记录。安装与使用问题请前往 [md2wechat 文档中心](https://www.md2wechat.cn/docs)。

## 提交事实修正

请附上：

- 需要修改的内容和当前问题
- 原始来源 URL
- 对应版本、tag 或完整 commit
- 核验日期
- 建议状态和触发重新检查的条件
- 受影响的页面

搜索摘要、模型回答、截图和转述可以帮助找到来源，但不能单独作为结论。

## 提交平台验证结果

平台状态升级需要脱敏证据。至少包含宿主安装、CLI 可见、`version --json`、`capabilities --json`、`inspect` 和预览结果。若要声明草稿能力，还需说明用户授权后的真实草稿闭环；创建草稿不等于发送或群发。

提交前运行：

```bash
node --test tests/*.test.mjs
node scripts/validate-evidence.mjs
```

## 安全与内容边界

不要提交真实 API Key、Token、AppID、AppSecret、Cookie、私钥、草稿 ID、未发布文章、未脱敏错误输出、本机绝对路径或私人信息。示例凭证使用明显占位符。

Wiki 维护事实、术语和来源；完整教程放在 Guide 或文档中心。没有原始来源的排名、数量、比较和最高级表述不会合并。

## 许可

提交到本仓库即表示你同意将贡献内容按 [CC BY 4.0](LICENSE.md)许可。引用第三方内容时，请确认其许可允许使用。
